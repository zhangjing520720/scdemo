eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8990/eureka/
server:
  port: 8993
spring:
  application:
    name: user
  zipkin: 
    base-url: http://localhost:8995
feign:
  hystrix:
    enabled: true
